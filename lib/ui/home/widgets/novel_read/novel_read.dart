import 'package:flutter/material.dart';

class NovelRead extends StatelessWidget {
  const NovelRead({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SingleChildScrollView(
      child: Text(''),
    );
  }
}
